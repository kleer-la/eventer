.controls
  .form-wrapper
    = render '/commons/notice'
    .alert.alert-info
      Transacción finalizada, en breve recibirás un e-mail con la confirmación.
    %br
    %table
      %tbody
        %tr
          %td
          %td
        - @data_to_show.each do |key,value|
          %tr
            %td= "#{key}: "
            %td= value

    %br
    %br

    %a{ href: "https://www.kleer.la/entrenamos/todos", title: "Volver a la agenda", class: "btn btn-primary"}
      Volver a la agenda
