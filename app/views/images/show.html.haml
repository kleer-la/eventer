.page-header
  %h1.pull-left
    %i.icon-open-folder
    %span Imágenes - Ver
- markdown = "![alt text](#{@public_url}  \"Titulo\")"
.row-fuid
  .span12.box
    %h4
      URL para colocar en campos cover o side image
    %p
      = @public_url
      %button.btn.btn-secondary{type: "button", onclick: "copyText('#{@public_url}')"} Copiar Clipboard
    %h4
      Markdown para colocar en campos description de Articulos y tipos de evento
    %p
      = markdown
      %button.btn.btn-secondary{type: "button", onclick: "copyText('#{markdown}')"} Copiar Clipboard
    %h4
      Imagen / PDF
    - if @public_url.end_with? '.pdf'
      %embed{ src: "#{@public_url}", type: "application/pdf", width: "100%", height: "600px" }
    - else
      %a{ :href => "" }
        %img{src: "#{@public_url}"}

= link_to 'Back', images_path

:javascript
  function copyText(textToCopy) {
      navigator.clipboard.writeText(textToCopy)
  };
