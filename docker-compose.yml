version: "3.3"
services:
  eventer:
    image: ruby:3.1
    container_name: eventer
    # env_file:
    #   - ./eventer.env
    ports:
      - '3000:3000'
    volumes:
      - .:/app
    tty: true
    working_dir: /app
